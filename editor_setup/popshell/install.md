### sudo apt install gnome-shell-extension-pop-shell # not avaiable in ubuntu 20

### prepare / manager tools
```sh
╰──> sudo apt install gnome-shell-extensions
```

### install tools higher than native typescript-version in ubuntu 20
```sh
sudo npm install -g typescript
```

### install pop-shell by open-source.
```sh
git clone https://github.com/pop-os/shell.git

git checkout master_focal

make local-install
# read Makefile for get more.
``` 